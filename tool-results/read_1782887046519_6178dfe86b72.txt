     1→# EDULEARN PRO — MASTER REPAIR, CROSS-PANEL SYNCHRONIZATION AND DEPLOYMENT PROMPT
     2→
     3→You are acting as a **senior Next.js architect, Prisma engineer, application-security engineer, QA lead and deployment reviewer**.
     4→
     5→You have access to:
     6→
     7→1. The complete EDULEARN PRO source-code workspace.
     8→2. The deep audit report named `EDULEARN_PRO_DEEP_MODULE_AUDIT.md`.
     9→3. The existing Prisma schema, database access layer, API routes, Public panel, Admin panel and Student panel.
    10→
    11→Your task is to **directly inspect, modify, test and complete the existing application**.
    12→
    13→Do not return only suggestions, sample code, pseudo-code or another audit. Apply the fixes to the real project.
    14→
    15→---
    16→
    17→# 1. Primary objective
    18→
    19→Make EDULEARN PRO deployment-ready by fixing:
    20→
    21→* Broken API routes.
    22→* Non-working modules.
    23→* Admin changes that do not reflect in Student or Public panels.
    24→* Test-attempt data-loss paths.
    25→* Hidden-result privacy leakage.
    26→* Missing video-progress functionality.
    27→* Incomplete course-to-batch assignment.
    28→* Authentication session-expiry problems.
    29→* Incorrect analytics.
    30→* Hardcoded branding and ignored settings.
    31→* Placeholder controls.
    32→* TypeScript errors hidden during builds.
    33→* Authorization and data-integrity defects.
    34→
    35→The final application must maintain one consistent flow:
    36→
    37→```text
    38→Admin mutation
    39→    ↓
    40→Database update
    41→    ↓
    42→Cache/state invalidation
    43→    ↓
    44→Updated Admin panel
    45→    ↓
    46→Updated Student/Public panel
    47→    ↓
    48→Direct URL and API access follow the same rule
    49→```
    50→
    51→A change is not fixed merely because the database updates. It is fixed only when every dependent panel, API and direct-access route reflects the same state.
    52→
    53→---
    54→
    55→# 2. Non-negotiable implementation rules
    56→
    57→## 2.1 Preserve the current application
    58→
    59→* Do not rebuild the project from scratch.
    60→* Do not replace the existing architecture unnecessarily.
    61→* Preserve working pages, styling, database records and existing functional workflows.
    62→* Make targeted, maintainable fixes.
    63→* Do not remove functional modules to make tests pass.
    64→* Do not silently change established business logic unless the audit identifies that logic as broken.
    65→* Do not replace real data with mock data.
    66→* Do not hardcode record IDs, test results, statuses, analytics or user values.
    67→
    68→## 2.2 No fake fixes
    69→
    70→Do not:
    71→
    72→* Comment out broken code.
    73→* Catch and suppress errors without resolving them.
    74→* return fake success responses.
    75→* add `any` everywhere to silence TypeScript.
    76→* leave TODO comments instead of implementation.
    77→* keep inactive duplicate handlers.
    78→* keep unreachable fallback branches.
    79→* keep `typescript.ignoreBuildErrors: true`.
    80→* mark a module fixed without running its acceptance tests.
    81→
    82→## 2.3 Data safety
    83→
    84→Before schema changes:
    85→
    86→* Inspect the existing Prisma schema and migration history.
    87→* Use additive, backward-compatible migrations wherever possible.
    88→* Preserve existing users, attempts, answers, courses, batches, enrollments and progress records.
    89→* Do not reset or reseed the production database.
    90→* Use Prisma transactions for multi-step state changes.
    91→* Make retry-sensitive actions idempotent.
    92→
    93→## 2.4 Route security
    94→
    95→Every Student API must verify:
    96→
    97→1. The current user is authenticated.
    98→2. The current user has the STUDENT role.
    99→3. The requested data belongs to or is accessible by that student.
   100→4. The associated batch is in an allowed lifecycle state.
   101→5. The associated course or test is published where required.
   102→6. Archived content is rejected.
   103→7. Direct API access follows the same rules as the UI.
   104→
   105→Every Admin API must verify the authenticated user has the ADMIN role.
   106→
   107→Return correct status codes:
   108→
   109→* `400` for invalid request data.
   110→* `401` for unauthenticated access.
   111→* `403` for authenticated but unauthorized access.
   112→* `404` when an accessible resource does not exist.
   113→* `409` for state conflicts or capacity conflicts.
   114→* `422` for valid JSON that violates business rules.
   115→
   116→---
   117→
   118→# 3. Required execution order
   119→
   120→Complete work in the following order.
   121→
   122→Do not begin cosmetic cleanup before deployment blockers are fixed.
   123→
   124→## Phase 1: Stop hard failures and data loss
   125→
   126→1. Fix all six student-status routes.
   127→2. Implement the missing student video-progress route.
   128→3. Repair public registration.
   129→4. Repair Contact PATCH.
   130→5. Repair Feedback PATCH.
   131→6. Repair test save, resume, exit and submission.
   132→7. Enforce hidden-result rules.
   133→8. Add Admin result publication.
   134→9. Connect course-to-batch assignment and unassignment.
   135→10. Fix Admin material download.
   136→11. Fix Student Feedback runtime failure.
   137→
   138→## Phase 2: Make all status changes propagate
   139→
   140→1. Centralize access rules.
   141→2. Apply them to all Student APIs.
   142→3. Fix batch lifecycle propagation.
   143→4. Fix course lifecycle propagation.
   144→5. Fix test lifecycle propagation.
   145→6. Fix material lifecycle propagation.
   146→7. Centralize branding/settings.
   147→8. Add state/cache invalidation after mutations.
   148→
   149→## Phase 3: Authentication, integrity and authorization
   150→
   151→1. Complete refresh-token flow.
   152→2. Remove insecure secret fallbacks.
   153→3. Make test submission idempotent.
   154→4. Enforce batch capacity transactionally.
   155→5. Protect Leaderboard batch access.
   156→6. Validate partial date updates.
   157→7. Fix audit-action accuracy.
   158→
   159→## Phase 4: Analytics and incomplete workflows
   160→
   161→1. Correct dashboard statistics.
   162→2. Deduplicate course progress.
   163→3. Complete Admin lifecycle controls.
   164→4. Replace or remove non-functional controls.
   165→5. Fix empty/loading states.
   166→6. Run full build and regression tests.
   167→
   168→---
   169→
   170→# 4. Exact required repairs
   171→
   172→## ISSUE 1 — Fix all six Admin student-status routes
   173→
   174→Affected actions:
   175→
   176→* Approve
   177→* Reject
   178→* Block
   179→* Unblock
   180→* Activate
   181→* Deactivate
   182→
   183→Affected route directories:
   184→
   185→```text
   186→src/app/api/admin/students/[id]/approve/
   187→src/app/api/admin/students/[id]/reject/
   188→src/app/api/admin/students/[id]/block/
   189→src/app/api/admin/students/[id]/unblock/
   190→src/app/api/admin/students/[id]/activate/
   191→src/app/api/admin/students/[id]/deactivate/
   192→```
   193→
   194→The existing helper receives a context object but incorrectly executes:
   195→
   196→```ts
   197→const { id } = await params
   198→```
   199→
   200→Use a correct Next.js App Router handler contract, for example:
   201→
   202→```ts
   203→export async function PATCH(
   204→  request: Request,
   205→  context: { params: Promise<{ id: string }> }
   206→) {
   207→  const { id } = await context.params;
   208→}
   209→```
   210→
   211→Alternatively, destructure `{ params }` correctly before passing it to a helper.
   212→
   213→Requirements:
   214→
   215→* Validate that `id` is a non-empty identifier.
   216→* Confirm the target student exists.
   217→* Confirm the transition is valid.
   218→* Update status transactionally.
   219→* Write the correct audit entry.
   220→* Return the updated student object.
   221→* Refresh the Admin list and detail state after success.
   222→* Ensure blocked, rejected, inactive or unapproved users cannot access Student APIs.
   223→* Ensure the currently logged-in target user loses access according to the intended session policy.
   224→
   225→Do not duplicate six large handlers. Extract a typed shared status-transition helper while preserving route-specific audit actions and validation.
   226→
   227→---
   228→
   229→## ISSUE 2 — Implement Student Video Progress GET and POST
   230→
   231→Create the missing route:
   232→
   233→```text
   234→src/app/api/student/videos/[id]/progress/route.ts
   235→```
   236→
   237→The existing Video Player calls:
   238→
   239→```text
   240→GET  /api/student/videos/{id}/progress
   241→POST /api/student/videos/{id}/progress
   242→```
   243→
   244→Implement both methods.
   245→
   246→### GET requirements
   247→
   248→Return sufficient data for the current player to initialize:
   249→
   250→* Video metadata.
   251→* Authorized course/batch context.
   252→* Existing last playback position.
   253→* Existing watched duration.
   254→* Completion state.
   255→* Effective completion threshold.
   256→* Any player fields currently expected by `video-player.tsx`.
   257→
   258→Validate:
   259→
   260→* Student authentication.
   261→* Video existence.
   262→* Course publication status.
   263→* Student enrollment.
   264→* Batch lifecycle status.
   265→* Video/archive visibility.
   266→* Direct URL access.
   267→
   268→### POST requirements
   269→
   270→Consume the existing progress heartbeat validation schema where appropriate.
   271→
   272→Safely process:
   273→
   274→* Current playback position.
   275→* Duration.
   276→* Watched seconds or heartbeat increment.
   277→* Completion state.
   278→* Last-watched timestamp.
   279→
   280→Requirements:
   281→
   282→* Clamp negative or invalid values.
   283→* Do not allow playback position above duration.
   284→* Prevent duplicate heartbeat requests from inflating watch time.
   285→* Upsert one progress record for the appropriate student/video relationship.
   286→* Apply the completion threshold from Admin settings.
   287→* Mark complete only after the configured threshold is reached.
   288→* Never move completed progress backward.
   289→* Return the updated normalized progress record.
   290→
   291→After implementation, verify that the following update correctly:
   292→
   293→* Continue Watching.
   294→* Student course progress.
   295→* Video completion percentage.
   296→* Admin total watch time.
   297→* Admin most-watched videos.
   298→* Student last playback position.
   299→* Student completion state.
   300→
   301→---
   302→
   303→## ISSUE 3 — Repair Public Registration
   304→
   305→Affected file:
   306→
   307→```text
   308→src/components/edulearn/public/register-page.tsx
   309→```
   310→
   311→### Password field
   312→
   313→Do not override the `onChange` supplied by React Hook Form after spreading `register()`.
   314→
   315→Either:
   316→
   317→* Compose the React Hook Form handler with the local password-strength handler, or
   318→* use `Controller`, `watch` or `setValue` correctly.
   319→
   320→React Hook Form must receive every password change.
   321→
   322→### Terms checkbox
   323→
   324→The Radix checkbox does not use the native checkbox event contract.
   325→
   326→Use `Controller` or explicit `checked` and `onCheckedChange` integration.
   327→
   328→The form value must be a real boolean and validation must require `true`.
   329→
   330→Requirements:
   331→
   332→* Valid registration submits successfully.
   333→* Invalid password is blocked.
   334→* Unchecked terms are blocked.
   335→* Checking the terms visually updates form state.
   336→* Server errors appear without clearing valid fields.
   337→* Duplicate submission is prevented.
   338→* No registration record is created twice from one action.
   339→
   340→Also connect Terms and Privacy buttons to real pages or remove the clickable appearance until those pages exist.
   341→
   342→---
   343→
   344→## ISSUE 4 — Fix Admin Contact status and notes update
   345→
   346→Current UI calls:
   347→
   348→```text
   349→PATCH /api/admin/contact/{id}
   350→```
   351→
   352→Move or implement PATCH inside:
   353→
   354→```text
   355→src/app/api/admin/contact/[id]/route.ts
   356→```
   357→
   358→The collection route must not pretend to receive a dynamic ID.
   359→
   360→Requirements:
   361→
   362→* Validate status values.
   363→* Validate notes length and type.
   364→* Update the selected message.
   365→* Return the updated object.
   366→* Write an audit record where appropriate.
   367→* Update Admin list and detail without stale status.
   368→
   369→Also fix GET behavior:
   370→
   371→The current GET marks a message READ but returns the old pre-update object. Return the post-update record.
   372→
   373→Avoid an unnecessary write when the message is already READ or later in the workflow.
   374→
   375→---
   376→
   377→## ISSUE 5 — Fix Admin Feedback status and notes update
   378→
   379→Current UI calls:
   380→
   381→```text
   382→PATCH /api/admin/feedback/{id}
   383→```
   384→
   385→Implement PATCH in:
   386→
   387→```text
   388→src/app/api/admin/feedback/[id]/route.ts
   389→```
   390→
   391→Requirements:
   392→
   393→* Validate allowed transitions.
   394→* Update REVIEWING, RESOLVED and CLOSED states correctly.
   395→* Save notes.
   396→* Return the updated feedback object.
   397→* Refresh both Admin list and detail state.
   398→* Remove the incorrectly placed dynamic PATCH behavior from the collection route.
   399→
   400→---
   401→
   402→## ISSUE 6 — Fix Test Save, Resume, Exit and Answer Data Loss
   403→
   404→Affected areas include:
   405→
   406→```text
   407→src/components/edulearn/student/pages/take-test.tsx
   408→src/app/api/student/tests/[id]/start/route.ts
   409→src/app/api/student/attempts/[id]/route.ts
   410→```
   411→
   412→### Resume requirements
   413→
   414→When an existing active attempt is resumed:
   415→
   416→* Return saved answers with question IDs and selected option IDs.
   417→* Initialize the client answer state from saved answers.
   418→* Preserve remaining time using server timestamps.
   419→* Preserve the original question and option order.
   420→* Do not reset answers to empty values.
   421→
   422→The client may either:
   423→
   424→* Receive saved answers in the start/resume response, or
   425→* call the attempt detail route immediately after resume.
   426→
   427→Use one clear contract rather than two conflicting flows.
   428→
   429→### Save Progress requirements
   430→
   431→* Upsert only answers supplied by the student.
   432→* Preserve previously saved answers not changed by the current request.
   433→* Allow an answer to be intentionally cleared through an explicit value.
   434→* Return the saved normalized answer state.
   435→* Prevent saving after submission.
   436→
   437→### Exit requirements
   438→
   439→The button says “Exit (saves progress).”
   440→
   441→It must actually:
   442→
   443→1. Save the current answer state.
   444→2. Confirm the save succeeded.
   445→3. Then navigate away.
   446→
   447→If saving fails, remain on the test and show an error.
   448→
   449→### Submit requirements
   450→
   451→Do not delete all saved answer rows and recreate blanks from incomplete client memory.
   452→
   453→On submission:
   454→
   455→* Merge the latest client answers with server-saved answers.
   456→* Evaluate the complete merged set.
   457→* Use a transaction.
   458→* Mark the attempt submitted once.
   459→* Prevent later mutation.
   460→* Preserve saved answers when the client omits unchanged questions.
   461→
   462→---
   463→
   464→## ISSUE 7 — Make Test Submission Idempotent
   465→
   466→Repeated submission currently returns a different response shape.
   467→
   468→Create one stable response contract.
   469→
   470→A first submit and a retry of the same already-submitted attempt must return compatible fields.
   471→
   472→The client must never receive undefined `test` or `questions` fields that it assumes exist.
   473→
   474→Requirements:
   475→
   476→* Disable the Submit button while a request is active.
   477→* Handle network retry safely.
   478→* Use a transaction and state check.
   479→* Do not calculate or store a second score.
   480→* Do not create duplicate answer records.
   481→* Return the same result-state contract for the original and repeated request.
   482→* Include an `alreadySubmitted` flag without removing required data.
   483→
   484→---
   485→
   486→## ISSUE 8 — Enforce hidden test results at API level
   487→
   488→`showResultImmediately=false` must be a real access rule, not a condition on one page.
   489→
   490→Add an explicit publication state. Prefer an additive field such as:
   491→
   492→```prisma
   493→resultPublishedAt DateTime?
   494→```
   495→
   496→on the appropriate attempt/result entity.
   497→
   498→The exact schema location must be selected after inspecting existing models, but it must support:
   499→
   500→* Immediate publication when `showResultImmediately=true`.
   501→* Hidden result after submission when it is false.
   502→* Later Admin publication.
   503→* Auditable publication time.
   504→* Protection per submitted attempt or a clearly defined bulk-publication operation.
   505→
   506→### Student API restrictions before publication
   507→
   508→Before a result is published, Student APIs must not expose:
   509→
   510→* Score.
   511→* Percentage.
   512→* Pass/fail.
   513→* Correct answer IDs.
   514→* Correct/incorrect question state.
   515→* Question-level marks.
   516→* Average or best-score contributions.
   517→* Detailed result routes.
   518→* Hidden values in embedded batch or test-history objects.
   519→
   520→Return only safe information such as:
   521→
   522→* Attempt ID.
   523→* Submission timestamp.
   524→* Publication pending state.
   525→* Test title.
   526→* Result availability status.
   527→
   528→The server, not the client, must remove protected fields.
   529→
   530→### Admin workflow
   531→
   532→Add Admin controls to:
   533→
   534→* Publish one result.
   535→* Publish selected results.
   536→* Publish all submitted results for a test.
   537→* See publication state and publication timestamp.
   538→
   539→Add protected Admin API endpoint(s) and correct audit records.
   540→
   541→### Student behavior
   542→
   543→* Hidden attempts display “Result awaiting publication.”
   544→* Direct result-detail URL returns a safe pending response or `403`.
   545→* Published attempts immediately become visible after refetch.
   546→* Existing immediate-result behavior remains unchanged.
   547→
   548→---
   549→
   550→## ISSUE 9 — Apply Test Shuffle Settings
   551→
   552→The system stores:
   553→
   554→* `shuffleQuestions`
   555→* `shuffleOptions`
   556→
   557→but does not use them.
   558→
   559→Requirements:
   560→
   561→* Expose the settings in Admin create/edit UI.
   562→* Apply question shuffling only when enabled.
   563→* Apply option shuffling only when enabled.
   564→* Preserve question and option order for the full lifetime of an attempt.
   565→* Do not reshuffle when the page refreshes or the attempt resumes.
   566→* Do not alter answer correctness relationships.
   567→
   568→Use either:
   569→
   570→* Persisted order, or
   571→* deterministic shuffling based on an immutable attempt-specific seed.
   572→
   573→Never call a fresh random shuffle on each render.
   574→
   575→---
   576→
   577→## ISSUE 10 — Connect Course-to-Batch Assignment
   578→
   579→Existing APIs include course/batch assignment routes, but no working Admin UI uses them.
   580→
   581→Implement a complete workflow in Admin Course Detail and/or Batch Detail:
   582→
   583→* View currently assigned batches.
   584→* Search/select eligible batches.
   585→* Assign one or multiple batches.
   586→* Unassign a course from a batch.
   587→* Prevent duplicate assignment.
   588→* Confirm destructive unassignment.
   589→* Show current course and batch lifecycle status.
   590→* Refresh related pages after mutation.
   591→
   592→Inspect existing route contracts first. Reuse them where correct. Repair or extend them if unassignment is missing.
   593→
   594→After assignment:
   595→
   596→* Published courses must appear for eligible students.
   597→* Draft or archived courses must not appear.
   598→* Inactive or completed batch behavior must follow the centralized access policy.
   599→* Direct course URLs must follow the same rule.
   600→
   601→Do not leave text saying “Assign courses from the Courses page” unless a working control exists there.
   602→
   603→---
   604→
   605→## ISSUE 11 — Fix Admin Material Download
   606→
   607→The UI currently calls:
   608→
   609→```text
   610→/api/admin/materials/{id}/download
   611→```
   612→
   613→while the actual GET handler is:
   614→
   615→```text
   616→/api/admin/materials/{id}
   617→```
   618→
   619→Choose one canonical route.
   620→
   621→Prefer keeping the existing working server route unless a separate `/download` route is justified.
   622→
   623→Update all callers.
   624→
   625→Requirements:
   626→
   627→* Admin download succeeds.
   628→* Filename and content type are correct.
   629→* Missing files return a clear error.
   630→* Unauthorized requests are rejected.
   631→* No dead duplicate download route remains.
   632→
   633→---
   634→
   635→## ISSUE 12 — Fix Student Feedback runtime error
   636→
   637→Affected file:
   638→
   639→```text
   640→src/components/edulearn/student/pages/feedback.tsx
   641→```
   642→
   643→Fix the missing toast import or use the project’s existing notification utility consistently.
   644→
   645→Also prevent duplicate creation:
   646→
   647→* Disable Submit while pending.
   648→* Clear the form only after confirmed success.
   649→* Show a success notification.
   650→* Preserve the form when the API fails.
   651→* Do not crash after a successful database insert.
   652→
   653→---
   654→
   655→# 5. Centralized cross-panel access policy
   656→
   657→Create a shared, typed server-side policy layer instead of duplicating different conditions across routes.
   658→
   659→Possible location:
   660→
   661→```text
   662→src/lib/access-policy.ts
   663→```
   664→
   665→The implementation may use helpers such as:
   666→
   667→```ts
   668→canStudentAccessBatch(...)
   669→canStudentAccessCourse(...)
   670→canStudentAccessTest(...)
   671→canStudentAccessMaterial(...)
   672→canStudentAccessVideo(...)
   673→canStudentViewResult(...)
   674→```
   675→
   676→Do not rely only on helpers returning booleans after loading unrestricted data. Where practical, encode the same restrictions in Prisma `where` clauses.
   677→
   678→## Batch access policy
   679→
   680→Define explicitly which statuses allow Student access.
   681→
   682→At minimum:
   683→
   684→* ACTIVE: accessible.
   685→* UPCOMING: accessible only where the product intentionally supports previews.
   686→* INACTIVE: no learning-content access.
   687→* COMPLETED: either read-only historical access or no access, but the rule must be consistent.
   688→* ARCHIVED or equivalent: no active access.
   689→
   690→Use the product’s existing intended lifecycle where it is clear. Document any unavoidable choice.
   691→
   692→Remove this defect completely:
   693→
   694→```ts
   695→bc.batch.status === 'ACTIVE' ||
   696→bc.batch.status === 'UPCOMING' ||
   697→true
   698→```
   699→
   700→No unconditional bypass may remain.
   701→
   702→## Course access policy
   703→
   704→A Student may access a course only when:
   705→
   706→* The student is valid and authorized.
   707→* The student has an allowed enrollment relationship.
   708→* The parent batch passes lifecycle rules.
   709→* The course is PUBLISHED.
   710→* The course is not archived/deleted.
   711→* The course assignment is active.
   712→
   713→Apply this to:
   714→
   715→* Course list.
   716→* Course detail.
   717→* Batch detail.
   718→* Dashboard.
   719→* Video access.
   720→* Material access.
   721→* Direct API URLs.
   722→
   723→## Test access policy
   724→
   725→Require:
   726→
   727→* Valid enrollment.
   728→* Allowed batch status.
   729→* Test status PUBLISHED.
   730→* Test assignment validity.
   731→* Test date/time availability.
   732→* Attempt-limit rules.
   733→* Result-publication rules.
   734→
   735→Batch detail must not show unpublished tests.
   736→
   737→## Material access policy
   738→
   739→Require:
   740→
   741→* Material not archived.
   742→* Valid material visibility rule.
   743→* Parent batch allowed.
   744→* Parent course published when course-scoped.
   745→* Student enrollment and assignment.
   746→* Direct download authorization.
   747→
   748→Deactivating a batch or archiving a course must revoke future file access according to policy.
   749→
   750→---
   751→
   752→# 6. Cross-panel synchronization and invalidation
   753→
   754→After every successful Admin mutation, update all affected views.
   755→
   756→Use the existing state architecture first. Do not add a heavy dependency solely for refetching.
   757→
   758→Apply suitable mechanisms such as:
   759→
   760→* Returning the updated object from mutation APIs.
   761→* Updating local list/detail state.
   762→* Calling `router.refresh()` for server-rendered data.
   763→* Re-fetching affected stores.
   764→* `revalidatePath` or `revalidateTag` for cached server data.
   765→* Revalidation on window focus.
   766→* A lightweight same-origin `BroadcastChannel` for open Admin/Student/Public tabs where appropriate.
   767→
   768→At minimum, synchronize:
   769→
   770→* Student status changes.
   771→* Batch status changes.
   772→* Course status and assignments.
   773→* Test status and assignments.
   774→* Test-result publication.
   775→* Materials.
   776→* Contact and Feedback status.
   777→* Branding and settings.
   778→* Announcements.
   779→
   780→Do not create infinite polling or fetch loops.
   781→
   782→An already-open panel must refresh through a defined strategy rather than relying permanently on manual browser reload.
   783→
   784→---
   785→
   786→# 7. Authentication and session continuity
   787→
   788→## Refresh route
   789→
   790→Implement the missing route:
   791→
   792→```text
   793→POST /api/auth/refresh
   794→```
   795→
   796→Use the existing refresh-session records rather than creating a second unrelated system.
   797→
   798→Requirements:
   799→
   800→* Read the refresh token securely.
   801→* Validate signature/hash/session record.
   802→* Reject expired, revoked or reused tokens.
   803→* Rotate the refresh token.
   804→* Revoke the previous refresh token.
   805→* Issue a fresh short-lived access token.
   806→* Update the secure cookie/session state.
   807→* Return a minimal safe response.
   808→* Protect against token replay.
   809→
   810→## API client behavior
   811→
   812→On an access-token `401` caused by expiry:
   813→
   814→1. Attempt refresh once.
   815→2. Retry the original request once.
   816→3. If refresh fails, clear authentication and redirect to login.
   817→4. Prevent multiple parallel failed requests from launching multiple refresh rotations.
   818→
   819→Do not create an infinite retry loop.
   820→
   821→## Security cleanup
   822→
   823→* Remove development fallback JWT secrets from source.
   824→* Require environment variables.
   825→* Fail startup clearly when required secrets are missing.
   826→* Use `httpOnly`, `secure` in production and appropriate `sameSite` settings for refresh cookies.
   827→* Do not expose refresh tokens to client JavaScript.
   828→* Ensure logout revokes the current refresh session.
   829→* Ensure password change revokes all refresh sessions.
   830→* Document the remaining access-token invalidation behavior if immediate JWT revocation is not implemented.
   831→
   832→Preserve the existing authentication UI unless a change is required for correctness.
   833→
   834→---
   835→
   836→# 8. Batch capacity and enrollment integrity
   837→
   838→The displayed batch capacity must be enforced.
   839→
   840→In student-assignment routes:
   841→
   842→* Count active/current enrollments.
   843→* Exclude students already assigned.
   844→* Validate the requested number against remaining capacity.
   845→* Perform check and insert in a transaction.
   846→* Protect against concurrent over-enrollment.
   847→* Return `409` when capacity would be exceeded.
   848→* Return which students were already assigned or invalid.
   849→* Do not partially assign unless the API explicitly supports and reports partial results.
   850→
   851→Admin UI requirements:
   852→
   853→* Display current count and remaining capacity.
   854→* Exclude invalid statuses where appropriate.
   855→* Exclude or mark already-enrolled students.
   856→* Support pagination/search instead of silently limiting the usable list to 100.
   857→* Disable assignment when the batch is full.
   858→
   859→---
   860→
   861→# 9. Leaderboard authorization
   862→
   863→When `/api/student/leaderboard` receives a `batchId`:
   864→
   865→* Verify the current student is enrolled in that batch.
   866→* Verify the enrollment and batch satisfy lifecycle access rules.
   867→* Reject unrelated batch IDs with `403` or a safe `404`.
   868→* Never expose another batch’s student names, images, scores or attempt data to an unauthorized student.
   869→
   870→If no `batchId` is supplied, return only leaderboards for batches accessible to the current student.
   871→
   872→---
   873→
   874→# 10. Branding and Settings propagation
   875→
   876→Centralize application settings and remove hardcoded branding where a stored setting exists.
   877→
   878→Apply configured values to:
   879→
   880→* Public header.
   881→* Public footer.
   882→* Public login/register branding.
   883→* Admin shell.
   884→* Student shell.
   885→* Loading/boot screen.
   886→* Browser title.
   887→* Description metadata.
   888→* OpenGraph metadata.
   889→* Twitter metadata.
   890→* Logo.
   891→* Favicon.
   892→* Institute name.
   893→* Tagline.
   894→
   895→Use valid fallback branding only when the database setting is genuinely absent.
   896→
   897→## Dynamic metadata
   898→
   899→Use an appropriate Next.js metadata strategy, such as `generateMetadata`, for server-accessible branding.
   900→
   901→Favicon changes must actually update browser metadata.
   902→
   903→## Maintenance mode
   904→
   905→Implement enforcement, not just storage.
   906→
   907→When maintenance mode is enabled:
   908→
   909→* Public learning/auth flows show a maintenance page.
   910→* Admin access remains available.
   911→* Existing privileged behavior is clearly defined.
   912→* APIs return a consistent maintenance response where required.
   913→
   914→## Settings-backed defaults
   915→
   916→Apply:
   917→
   918→* Video completion threshold.
   919→* Default maximum attempts in new test forms.
   920→* Maximum upload size in both server validation and Admin UI text.
   921→
   922→Do not show a hardcoded “20 MB” label when the server uses another limit.
   923→
   924→For settings such as Revenue or Certificates that have no delivered module:
   925→
   926→* Do not pretend they are active.
   927→* Either implement their real behavior or clearly label/remove unavailable controls.
   928→
   929→---
   930→
   931→# 11. Fix profile update navigation
   932→
   933→Affected file:
   934→
   935→```text
   936→src/components/edulearn/student/pages/profile.tsx
   937→```
   938→
   939→Do not call a global bootstrap operation that resets the current Student view after a normal profile save.
   940→
   941→Instead:
   942→
   943→* Return the updated user from the API.
   944→* Merge it into the shared auth/user store.
   945→* Refresh profile/header identity.
   946→* Keep the user on the Profile page.
   947→* Show success or failure state.
   948→
   949→Use global bootstrap only for true authentication initialization.
   950→
   951→---
   952→
   953→# 12. Analytics corrections
   954→
   955→## Admin approved-student trend
   956→
   957→Remove hardcoded:
   958→
   959→```ts
   960→approved: 0
   961→```
   962→
   963→Calculate approved students for each intended date interval using actual approval timestamps/status history available in the schema.
   964→
   965→If approval history is not currently stored, add the smallest correct auditable field/event required rather than fabricating data.
   966→
   967→## Admin aggregate counts
   968→
   969→Clearly define dashboard counts.
   970→
   971→Prefer separate values such as:
   972→
   973→* Total records.
   974→* Active records.
   975→* Published records.
   976→* Archived records.
   977→
   978→Do not label unrestricted counts in a way that implies only active content.
   979→
   980→## Student test statistics
   981→
   982→Use one consistent submitted-attempt dataset for:
   983→
   984→* Dashboard average.
   985→* Dashboard best.
   986→* Results average.
   987→* Results best.
   988→* Attempt count.
   989→
   990→Do not calculate Dashboard statistics from only the latest ten attempts unless the UI explicitly says “last ten attempts.”
   991→
   992→Pagination may limit displayed rows but must not limit aggregate calculations.
   993→
   994→Hidden unpublished results must not contribute visible score statistics before publication.
   995→
   996→## Course-progress deduplication
   997→
   998→Deduplicate courses by course ID when a course is assigned through multiple enrolled batches.
   999→
  1000→Do not produce duplicate cards or duplicate React keys.
  1001→
  1002→Define how progress is combined when the same course has multiple valid assignments.
  1003→
  1004→## Video analytics
  1005→
  1006→After implementing the progress endpoint, verify actual records drive:
  1007→
  1008→* Total watch time.
  1009→* Completion count.
  1010→* Continue Watching.
  1011→* Most-watched videos.
  1012→* Student course progress.
  1013→
  1014→Do not leave analytics silently showing zeros because of missing ingestion.
  1015→
  1016→---
  1017→
  1018→# 13. Complete Admin content lifecycle controls
  1019→
  1020→Inspect existing APIs and connect all intended lifecycle actions to UI.
  1021→
  1022→At minimum:
  1023→
  1024→## Courses
  1025→
  1026→* Create.
  1027→* Edit.
  1028→* Publish.
  1029→* Archive.
  1030→* Restore if supported.
  1031→* Assign batches.
  1032→* Unassign batches.
  1033→* View assigned batches.
  1034→
  1035→## Batches
  1036→
  1037→* Create.
  1038→* Edit.
  1039→* Change status.
  1040→* Archive.
  1041→* Assign students.
  1042→* Remove students.
  1043→* Assign courses if supported from this page.
  1044→* Enforce capacity.
  1045→
  1046→## Tests
  1047→
  1048→* Create.
  1049→* Edit.
  1050→* Publish.
  1051→* Archive.
  1052→* Configure shuffling.
  1053→* Configure result visibility.
  1054→* Assign/unassign batches.
  1055→* Publish results.
  1056→* View publication state.
  1057→
  1058→## Questions
  1059→
  1060→* Create.
  1061→* Edit.
  1062→* Delete with confirmation.
  1063→* Reorder.
  1064→* Preserve correct-answer mapping.
  1065→* Prevent edits that corrupt already-submitted attempts, or version the necessary data.
  1066→
  1067→Do not leave working APIs inaccessible behind missing UI controls.
  1068→
  1069→---
  1070→
  1071→# 14. Public enrollment request workflow
  1072→
  1073→The UI currently presents “Enroll Now” or “Request Enrollment” without a real request module.
  1074→
  1075→Choose one honest implementation:
  1076→
  1077→## Preferred: implement enrollment requests
  1078→
  1079→Add:
  1080→
  1081→* Student/public request endpoint.
  1082→* Duplicate-request prevention.
  1083→* Request status model.
  1084→* Admin request queue.
  1085→* Approve/reject actions.
  1086→* Capacity and student-status checks.
  1087→* Conversion of an approved request into enrollment.
  1088→* Notifications or visible request-state feedback.
  1089→
  1090→## Alternative
  1091→
  1092→If enrollment requests are outside the actual product scope:
  1093→
  1094→* Rename buttons to honest actions such as “Register to view available batches.”
  1095→* Remove wording that implies a request was created.
  1096→* Do not leave a fake “Request Enrollment” action.
  1097→
  1098→Do not invent a fake success notification.
  1099→
  1100→---
  1101→
  1102→# 15. Placeholder controls
  1103→
  1104→Review:
  1105→
  1106→* Admin search.
  1107→* Student search.
  1108→* Notification icons.
  1109→* Terms.
  1110→* Privacy Policy.
  1111→* Static notification badges.
  1112→
  1113→For each control:
  1114→
  1115→* Implement the real function, or
  1116→* remove/disable the interactive appearance.
  1117→
  1118→Do not ship buttons that look operational but have no handler.
  1119→
  1120→Terms and Privacy pages must be accessible from Registration and Footer if users are required to accept them.
  1121→
  1122→---
  1123→
  1124→# 16. Homepage empty state
  1125→
  1126→Do not use `batches.length === 0` as both loading and empty state.
  1127→
  1128→Maintain explicit states:
  1129→
  1130→```ts
  1131→loading
  1132→error
  1133→data
  1134→empty
  1135→```
  1136→
  1137→When the API successfully returns zero featured batches, show a real empty-state message rather than permanent skeleton loading.
  1138→
  1139→Apply the same correction to other pages with this anti-pattern.
  1140→
  1141→---
  1142→
  1143→# 17. Partial date-range validation
  1144→
  1145→Affected batch and test PATCH handlers must validate the final merged entity, not only fields present together in the incoming payload.
  1146→
  1147→Example:
  1148→
  1149→1. Load the current record.
  1150→2. Merge current dates with incoming dates.
  1151→3. Validate final start/end relationship.
  1152→4. Reject invalid ranges before update.
  1153→
  1154→Also validate:
  1155→
  1156→* Attempt windows.
  1157→* Registration windows where applicable.
  1158→* Start date before end date.
  1159→* Time zone consistency.
  1160→
  1161→---
  1162→
  1163→# 18. Audit history accuracy
  1164→
  1165→The Test update route currently writes `TEST_CREATED`.
  1166→
  1167→Use the correct update audit action.
  1168→
  1169→Review related lifecycle mutations for similar incorrect actions.
  1170→
  1171→Audit entries should contain:
  1172→
  1173→* Correct actor.
  1174→* Correct action.
  1175→* Correct entity type.
  1176→* Correct entity ID.
  1177→* Meaningful changed fields where supported.
  1178→* Timestamp.
  1179→
  1180→Do not record a creation event for an update.
  1181→
  1182→---
  1183→
  1184→# 19. Fix batch-detail material relation
  1185→
  1186→The current relation includes:
  1187→
  1188→```ts
  1189→OR: [
  1190→  { batchId: id },
  1191→  { visibility: 'COURSE' }
  1192→]
  1193→```
  1194→
  1195→inside a relation already constrained to the batch, so the COURSE branch does not retrieve intended course-only materials.
  1196→
  1197→Rewrite the query according to the actual visibility model.
  1198→
  1199→The result must intentionally include only materials the Student may access through:
  1200→
  1201→* The current batch, or
  1202→* Published courses assigned to the current batch,
  1203→
  1204→while applying archive, course-status and batch-status policies.
  1205→
  1206→Do not broaden access to unrelated COURSE-visible materials.
  1207→
  1208→---
  1209→
  1210→# 20. TypeScript and deployment requirements
  1211→
  1212→Remove from `next.config.ts`:
  1213→
  1214→```ts
  1215→typescript: {
  1216→  ignoreBuildErrors: true
  1217→}
  1218→```
  1219→
  1220→Fix every real TypeScript error, including:
  1221→
  1222→* Incorrect student-status route context typing.
  1223→* Missing `PublicSettings` export or incorrect imports.
  1224→* Missing `toast` symbol.
  1225→* Invalid `settings` prop passed to RegisterPage.
  1226→* Any new errors introduced during repairs.
  1227→
  1228→Also run:
  1229→
  1230→```bash
  1231→npm run lint
  1232→npx tsc --noEmit
  1233→npm run build
  1234→```
  1235→
  1236→Use the actual package manager and scripts declared by the project.
  1237→
  1238→A successful build with errors ignored is not accepted.
  1239→
  1240→Do not disable ESLint or TypeScript checks globally.
  1241→
  1242→---
  1243→
  1244→# 21. API and client contract standards
  1245→
  1246→For every repaired endpoint:
  1247→
  1248→* Define typed request payloads.
  1249→* Validate them.
  1250→* Return a stable typed response.
  1251→* Keep list/detail response shapes consistent where appropriate.
  1252→* Do not return stale pre-update objects.
  1253→* Do not expose private internal fields.
  1254→* Handle expected conflict states explicitly.
  1255→* Ensure the client checks `response.ok`.
  1256→* Show server error messages safely.
  1257→* Prevent duplicate pending actions.
  1258→* Refresh affected local and shared state after success.
  1259→
  1260→Remove dead collection-route handlers after moving them to dynamic routes.
  1261→
  1262→---
  1263→
  1264→# 22. Required automated and manual regression tests
  1265→
  1266→Create tests using the project’s existing testing stack. If no test stack exists, add the smallest suitable setup without introducing unnecessary complexity.
  1267→
  1268→## Authentication
  1269→
  1270→1. Valid registration creates one pending student.
  1271→2. Password changes reach React Hook Form.
  1272→3. Terms must be true.
  1273→4. Admin can approve, reject, block, unblock, activate and deactivate the selected student.
  1274→5. Invalid student ID does not modify another user.
  1275→6. Access token expiry triggers one refresh and request retry.
  1276→7. Revoked refresh token is rejected.
  1277→8. Logout revokes the session.
  1278→
  1279→## Course and batch propagation
  1280→
  1281→1. Assign course to batch.
  1282→2. Eligible student sees it.
  1283→3. Unassign course.
  1284→4. Student no longer sees or opens it.
  1285→5. Set course DRAFT or ARCHIVED.
  1286→6. It disappears from Batch Detail, Course List, Dashboard and direct URL.
  1287→7. Set batch INACTIVE.
  1288→8. Courses, tests, videos and materials obey the same access rule.
  1289→9. Restore allowed status.
  1290→10. Content becomes accessible after refresh/revalidation.
  1291→
  1292→## Video progress
  1293→
  1294→1. Open an authorized video.
  1295→2. GET returns video and current progress.
  1296→3. Send heartbeat.
  1297→4. Resume from stored position.
  1298→5. Reach configured completion threshold.
  1299→6. Completion becomes true once.
  1300→7. Repeated heartbeat does not inflate time incorrectly.
  1301→8. Admin analytics update.
  1302→
  1303→## Tests
  1304→
  1305→1. Start attempt.
  1306→2. Answer multiple questions.
  1307→3. Save progress.
  1308→4. Exit through the save-and-exit button.
  1309→5. Reopen attempt.
  1310→6. Confirm all saved answers are restored.
  1311→7. Change one answer.
  1312→8. Submit.
  1313→9. Confirm untouched saved answers remain.
  1314→10. Retry submission.
  1315→11. Confirm identical compatible response and no duplicate grading.
  1316→12. Confirm shuffled order remains stable across reload.
  1317→13. Confirm non-shuffled test remains ordered.
  1318→
  1319→## Hidden results
  1320→
  1321→1. Submit a test with immediate results disabled.
  1322→2. Confirm score is absent from submit response.
  1323→3. Confirm score is absent from Results API.
  1324→4. Confirm average/best excludes hidden score.
  1325→5. Confirm direct result URL does not expose it.
  1326→6. Publish from Admin.
  1327→7. Confirm the Student can then see it everywhere.
  1328→
  1329→## Contact and Feedback
  1330→
  1331→1. Update status.
  1332→2. Update notes.
  1333→3. Confirm dynamic PATCH succeeds.
  1334→4. Confirm list and detail show the same updated object.
  1335→5. Confirm Student Feedback does not crash or create duplicates.
  1336→
  1337→## Capacity and authorization
  1338→
  1339→1. Fill batch to capacity.
  1340→2. Attempt another assignment.
  1341→3. Confirm `409` and no partial over-enrollment.
  1342→4. Simulate concurrent enrollment attempts.
  1343→5. Confirm final count never exceeds capacity.
  1344→6. Query an unrelated leaderboard batch.
  1345→7. Confirm access is denied.
  1346→
  1347→## Settings and branding
  1348→
  1349→1. Change institute name.
  1350→2. Change tagline.
  1351→3. Change logo.
  1352→4. Change favicon.
  1353→5. Change upload limit.
  1354→6. Change test-attempt default.
  1355→7. Change video threshold.
  1356→8. Enable maintenance mode.
  1357→9. Verify Public, Admin, Student, metadata and APIs reflect the appropriate values.
  1358→
  1359→## Build
  1360→
  1361→1. Type-check with no suppression.
  1362→2. Lint.
  1363→3. Production build.
  1364→4. No ignored build errors.
  1365→5. No unresolved imports.
  1366→6. No missing route handlers.
  1367→
  1368→---
  1369→
  1370→# 23. Completion criteria
  1371→
  1372→Do not mark the project complete until all of the following are true:
  1373→
  1374→* Every audited hard failure is repaired.
  1375→* No test answer can be lost during save/resume/submit.
  1376→* Hidden test results are protected server-side.
  1377→* Video progress and analytics work end-to-end.
  1378→* Course-to-batch assignment is available in Admin UI.
  1379→* Admin status changes propagate consistently.
  1380→* Direct URLs cannot bypass status rules.
  1381→* Refresh-token flow works.
  1382→* Batch capacity is enforced transactionally.
  1383→* Leaderboard access is authorized.
  1384→* Branding/settings are no longer ignored.
  1385→* Analytics are based on real, consistent datasets.
  1386→* Placeholder controls are implemented or removed.
  1387→* TypeScript errors are no longer suppressed.
  1388→* Lint, type-check and production build pass.
  1389→* Regression evidence is provided.
  1390→
  1391→---
  1392→
  1393→# 24. Required final response after implementation
  1394→
  1395→After modifying the project, return a structured completion report containing:
  1396→
  1397→## A. Files changed
  1398→
  1399→List every created, modified and deleted file.
  1400→
  1401→## B. Database changes
  1402→
  1403→List:
  1404→
  1405→* Prisma model changes.
  1406→* Migration names.
  1407→* Backfill behavior.
  1408→* Why existing data remains safe.
  1409→
  1410→## C. Issue-by-issue status
  1411→
  1412→For each of the 36 audited findings, state:
  1413→
  1414→* Fixed.
  1415→* Removed because it was a fake/incomplete control.
  1416→* Partially fixed, with an exact reason.
  1417→* Not fixed, with reproducible evidence.
  1418→
  1419→Do not use vague statements such as “handled” or “improved.”
  1420→
  1421→## D. Cross-panel verification
  1422→
  1423→For every important Admin action, show:
  1424→
  1425→```text
  1426→Admin action
  1427→→ API mutation
  1428→→ database state
  1429→→ Admin refetch
  1430→→ Student/Public refetch
  1431→→ direct URL result
  1432→```
  1433→
  1434→## E. Test evidence
  1435→
  1436→Provide:
  1437→
  1438→* Commands executed.
  1439→* TypeScript result.
  1440→* Lint result.
  1441→* Production build result.
  1442→* Automated test result.
  1443→* Manual workflow result.
  1444→
  1445→## F. Remaining risks
  1446→
  1447→Report any unresolved limitation honestly.
  1448→
  1449→Do not declare “deployment-ready” when build or critical regression tests still fail.
  1450→
  1451→---
  1452→
  1453→# Final instruction
  1454→
  1455→Start by reading the complete audit and tracing the existing code contracts. Then implement the fixes in the specified phases.
  1456→
  1457→Do not produce another theoretical plan.
  1458→
  1459→Do not stop after repairing only the first visible error.
  1460→
  1461→Repair the full workflow so that Public, Admin, Student, APIs, Prisma data and direct URL access all enforce the same business state.
  1462→